# Dokumentation: JppTest

**Datei:** `a1aeins/ah_test.cpp`  
**Klasse:** `JppTest`  
**Zweck:** allgemeine Testereien mit JPP-Objekten  
**Verwendung:** Über JPL/JPP-Interface  

> ⚙️ Automatisch aus dem Quellcode generiert (JPP_METHOD / JPP_*-Parameter / JPP_GetParam). Beschreibungen mit _(ergänzt)_ stammen aus `beschreibungen.json`. Generierte Dateien nicht von Hand editieren — bei Code-Änderungen neu generieren.

---

## Übersicht

allgemeine Testereien mit JPP-Objekten

---

## Methodenübersicht

### JppTestMethode

**Kurzbeschreibung:**  
Liest JPP-Parameter owner/name, setzt per JVARS::SET die JVar auf "JppTest lebt!" und gibt long 1 zurück (Testmethode). _(ergänzt)_

| Parameter | Typ | Pflicht | Default |
|-----------|-----|---------|---------|
| owner | long | optional | `0` |
| name | string | optional | `JppTestMethode` |

**Rückgabe:** long

---

**Generiert mit `generate_jpp_docs.py` aus dem A.eins-Quellcode.**
